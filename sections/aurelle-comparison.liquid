{%- style -%}
  /* ======================================================================
     Aurelle Comparison Table
     Aurelle vs. typical skincare brands
     ====================================================================== */

  .aurelle-comparison {
    background-color: var(--aurelle-offwhite);
  }

  .aurelle-comparison__inner {
    max-width: var(--aurelle-container-max);
    margin: 0 auto;
    padding: var(--aurelle-section-padding-mobile) var(--aurelle-container-padding-mobile);
  }

  @media screen and (min-width: 768px) {
    .aurelle-comparison__inner {
      padding: var(--aurelle-section-padding-desktop) var(--aurelle-container-padding);
    }
  }

  .aurelle-comparison__header {
    text-align: center;
    margin-bottom: var(--aurelle-space-3xl);
  }

  .aurelle-comparison__label {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-semibold);
    font-size: var(--aurelle-text-xs);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--aurelle-gold);
    display: block;
    margin-bottom: var(--aurelle-space-sm);
  }

  .aurelle-comparison__title {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-bold);
    font-size: var(--aurelle-text-3xl);
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--aurelle-text);
    margin: 0 0 var(--aurelle-space-md) 0;
  }

  .aurelle-comparison__subtitle {
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-md);
    color: var(--aurelle-text-secondary);
    line-height: 1.7;
    max-width: 560px;
    margin: 0 auto;
  }

  /* Table wrapper */
  .aurelle-comparison__table-wrap {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }

  .aurelle-comparison__table {
    width: 100%;
    min-width: 560px;
    border-collapse: collapse;
  }

  /* Column headers */
  .aurelle-comparison__thead th {
    padding: var(--aurelle-space-md) var(--aurelle-space-xl);
    text-align: center;
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-semibold);
    font-size: var(--aurelle-text-sm);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .aurelle-comparison__thead th:first-child {
    text-align: left;
    color: var(--aurelle-text-muted);
  }

  .aurelle-comparison__col-aurelle {
    background-color: var(--aurelle-dark);
    color: var(--aurelle-offwhite) !important;
    border-radius: 12px 12px 0 0;
    position: relative;
  }

  .aurelle-comparison__col-aurelle::after {
    content: '';
    position: absolute;
    inset: 0;
    border: 2px solid var(--aurelle-gold);
    border-bottom: none;
    border-radius: 12px 12px 0 0;
    pointer-events: none;
  }

  .aurelle-comparison__col-others {
    color: var(--aurelle-text-muted) !important;
  }

  /* Rows */
  .aurelle-comparison__row td {
    padding: var(--aurelle-space-md) var(--aurelle-space-xl);
    text-align: center;
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-md);
    color: var(--aurelle-text-secondary);
    border-bottom: 1px solid var(--aurelle-border-light);
  }

  .aurelle-comparison__row td:first-child {
    text-align: left;
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-medium);
    font-size: var(--aurelle-text-md);
    color: var(--aurelle-text);
  }

  .aurelle-comparison__row:last-child td {
    border-bottom: none;
  }

  /* Aurelle column body cells */
  .aurelle-comparison__row td.aurelle-comparison__cell-aurelle {
    background-color: rgba(28, 28, 28, 0.04);
    position: relative;
    font-weight: var(--aurelle-weight-semibold);
    color: var(--aurelle-dark);
  }

  .aurelle-comparison__row td.aurelle-comparison__cell-aurelle::after {
    content: '';
    position: absolute;
    inset: 0;
    border-left: 2px solid var(--aurelle-gold);
    border-right: 2px solid var(--aurelle-gold);
    pointer-events: none;
  }

  /* Last row Aurelle cell gets bottom border */
  .aurelle-comparison__row:last-child td.aurelle-comparison__cell-aurelle::after {
    border-bottom: 2px solid var(--aurelle-gold);
    border-radius: 0 0 12px 12px;
  }

  /* Icons */
  .aurelle-comparison__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 50%;
  }

  .aurelle-comparison__icon--check {
    background-color: rgba(184, 149, 106, 0.12);
    color: var(--aurelle-gold);
  }

  .aurelle-comparison__icon--no {
    background-color: rgba(200, 80, 80, 0.08);
    color: #C85050;
  }

  .aurelle-comparison__icon--partial {
    background-color: rgba(180, 150, 80, 0.1);
    color: #B49650;
  }

  .aurelle-comparison__icon svg {
    width: 16px;
    height: 16px;
    stroke: currentColor;
    fill: none;
    stroke-width: 2.5;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  /* CTA below table */
  .aurelle-comparison__cta {
    text-align: center;
    margin-top: var(--aurelle-space-3xl);
  }

  .aurelle-comparison__cta-text {
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-md);
    color: var(--aurelle-text-secondary);
    margin-bottom: var(--aurelle-space-lg);
  }
{%- endstyle -%}

<section class="aurelle-comparison" id="aurelle-comparison-{{ section.id }}">
  <div class="aurelle-comparison__inner">

    <div class="aurelle-comparison__header">
      {%- if section.settings.label != blank -%}
        <span class="aurelle-comparison__label">{{ section.settings.label }}</span>
      {%- endif -%}
      <h2 class="aurelle-comparison__title">{{ section.settings.heading }}</h2>
      {%- if section.settings.subtitle != blank -%}
        <p class="aurelle-comparison__subtitle">{{ section.settings.subtitle }}</p>
      {%- endif -%}
    </div>

    <div class="aurelle-comparison__table-wrap">
      <table class="aurelle-comparison__table">
        <thead class="aurelle-comparison__thead">
          <tr>
            <th></th>
            <th class="aurelle-comparison__col-aurelle">{{ section.settings.col_aurelle | default: 'Aurelle' }}</th>
            <th class="aurelle-comparison__col-others">{{ section.settings.col_others | default: 'Typical Brands' }}</th>
          </tr>
        </thead>
        <tbody>
          {%- for block in section.blocks -%}
            <tr class="aurelle-comparison__row" {{ block.shopify_attributes }}>
              <td>{{ block.settings.feature }}</td>
              <td class="aurelle-comparison__cell-aurelle">
                {%- if block.settings.aurelle_value != blank -%}
                  {{ block.settings.aurelle_value }}
                {%- elsif block.settings.aurelle_check -%}
                  <span class="aurelle-comparison__icon aurelle-comparison__icon--check">
                    <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
                  </span>
                {%- else -%}
                  <span class="aurelle-comparison__icon aurelle-comparison__icon--no">
                    <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                  </span>
                {%- endif -%}
              </td>
              <td>
                {%- if block.settings.others_value != blank -%}
                  {{ block.settings.others_value }}
                {%- elsif block.settings.others_partial -%}
                  <span class="aurelle-comparison__icon aurelle-comparison__icon--partial">
                    <svg viewBox="0 0 24 24"><line x1="5" y1="12" x2="19" y2="12"/></svg>
                  </span>
                {%- else -%}
                  <span class="aurelle-comparison__icon aurelle-comparison__icon--no">
                    <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                  </span>
                {%- endif -%}
              </td>
            </tr>
          {%- endfor -%}
        </tbody>
      </table>
    </div>

    {%- if section.settings.cta_label != blank -%}
      <div class="aurelle-comparison__cta">
        {%- if section.settings.cta_subtext != blank -%}
          <p class="aurelle-comparison__cta-text">{{ section.settings.cta_subtext }}</p>
        {%- endif -%}
        <a href="{{ section.settings.cta_link }}" class="aurelle-btn aurelle-btn--gold aurelle-btn--lg">
          {{ section.settings.cta_label }}
        </a>
      </div>
    {%- endif -%}

  </div>
</section>

{% schema %}
{
  "name": "Aurelle Comparison",
  "tag": "section",
  "class": "aurelle-comparison-section",
  "settings": [
    {
      "type": "text",
      "id": "label",
      "label": "Label",
      "default": "The Difference"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Aurelle vs. The Rest"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Not all skincare is created equal. Here's why Aurelle stands apart."
    },
    {
      "type": "text",
      "id": "col_aurelle",
      "label": "Aurelle column label",
      "default": "Aurelle"
    },
    {
      "type": "text",
      "id": "col_others",
      "label": "Others column label",
      "default": "Typical Brands"
    },
    {
      "type": "header",
      "content": "Call to Action"
    },
    {
      "type": "text",
      "id": "cta_subtext",
      "label": "CTA subtext",
      "default": "Ready to experience the Aurelle difference?"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "Button link"
    }
  ],
  "blocks": [
    {
      "type": "row",
      "name": "Comparison Row",
      "settings": [
        {
          "type": "text",
          "id": "feature",
          "label": "Feature name",
          "default": "Clean Ingredients"
        },
        {
          "type": "header",
          "content": "Aurelle column"
        },
        {
          "type": "checkbox",
          "id": "aurelle_check",
          "label": "Show checkmark",
          "default": true,
          "info": "Overridden if custom value is set"
        },
        {
          "type": "text",
          "id": "aurelle_value",
          "label": "Custom value (optional)",
          "info": "Leave blank to use checkmark/X icon"
        },
        {
          "type": "header",
          "content": "Others column"
        },
        {
          "type": "checkbox",
          "id": "others_partial",
          "label": "Show partial (—) instead of X",
          "default": false
        },
        {
          "type": "text",
          "id": "others_value",
          "label": "Custom value (optional)",
          "info": "Leave blank to use X or partial icon"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Aurelle Comparison",
      "blocks": [
        { "type": "row", "settings": { "feature": "Clean, Transparent Ingredients", "aurelle_check": true, "others_partial": true } },
        { "type": "row", "settings": { "feature": "Dermatologist Tested & Approved", "aurelle_check": true, "others_partial": false } },
        { "type": "row", "settings": { "feature": "Cruelty-Free & Vegan Formula", "aurelle_check": true, "others_partial": false } },
        { "type": "row", "settings": { "feature": "No Harsh Chemicals or Parabens", "aurelle_check": true, "others_partial": false } },
        { "type": "row", "settings": { "feature": "30-Day Money-Back Guarantee", "aurelle_check": true, "others_partial": false } },
        { "type": "row", "settings": { "feature": "Sustainably Sourced & Packaged", "aurelle_check": true, "others_partial": true } }
      ]
    }
  ]
}
{% endschema %}
