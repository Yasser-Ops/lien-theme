{{ 'lien-base.css' | asset_url | stylesheet_tag }}

{%- style -%}
  /* ======================================================================
     Lien Hero Section
     Split layout: image left, content right
     ====================================================================== */

  .lien-hero {
    background-color: var(--lien-offwhite);
    overflow: hidden;
  }

  .lien-hero__inner {
    display: grid;
    grid-template-columns: 1fr;
    min-height: 85vh;
  }

  @media screen and (min-width: 1024px) {
    .lien-hero__inner {
      grid-template-columns: 1fr 1fr;
      min-height: 90vh;
    }
  }

  /* Image side */
  .lien-hero__media {
    position: relative;
    overflow: hidden;
    min-height: 50vh;
    background-color: var(--lien-cream);
  }

  @media screen and (min-width: 1024px) {
    .lien-hero__media {
      min-height: auto;
    }
  }

  .lien-hero__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .lien-hero__placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(145deg, var(--lien-cream) 0%, var(--lien-cream-dark) 100%);
  }

  .lien-hero__placeholder svg {
    width: 80px;
    height: 80px;
    opacity: 0.3;
    color: var(--lien-accent);
  }

  /* Content side */
  .lien-hero__content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: var(--lien-space-2xl) var(--lien-container-padding-mobile);
  }

  @media screen and (min-width: 1024px) {
    .lien-hero__content {
      padding: var(--lien-space-4xl) var(--lien-space-4xl);
      padding-right: var(--lien-space-3xl);
    }
  }

  .lien-hero__label {
    font-family: var(--lien-font-heading);
    font-weight: var(--lien-weight-semibold);
    font-size: var(--lien-text-xs);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--lien-accent);
    margin-bottom: var(--lien-space-lg);
    opacity: 0;
    animation: lien-fade-in 0.6s ease 0.2s forwards;
  }

  .lien-hero__title {
    font-family: var(--lien-font-heading);
    font-weight: var(--lien-weight-extrabold);
    font-size: var(--lien-text-4xl);
    line-height: 1.05;
    letter-spacing: -0.03em;
    color: var(--lien-text);
    margin: 0 0 var(--lien-space-lg) 0;
    opacity: 0;
    animation: lien-fade-in 0.7s ease 0.3s forwards;
  }

  @media screen and (min-width: 1024px) {
    .lien-hero__title {
      font-size: var(--lien-text-5xl);
    }
  }

  .lien-hero__subtitle {
    font-family: var(--lien-font-body);
    font-size: var(--lien-text-md);
    color: var(--lien-text-secondary);
    line-height: 1.7;
    margin: 0 0 var(--lien-space-xl) 0;
    max-width: 480px;
    opacity: 0;
    animation: lien-fade-in 0.7s ease 0.4s forwards;
  }

  .lien-hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--lien-space-md);
    margin-bottom: var(--lien-space-2xl);
    opacity: 0;
    animation: lien-fade-in 0.7s ease 0.5s forwards;
  }

  .lien-hero__proof {
    display: flex;
    align-items: center;
    gap: var(--lien-space-sm);
    opacity: 0;
    animation: lien-fade-in 0.7s ease 0.6s forwards;
  }

  .lien-hero__proof-stars {
    display: flex;
    gap: 2px;
    color: var(--lien-accent);
  }

  .lien-hero__proof-stars svg {
    width: 16px;
    height: 16px;
    fill: currentColor;
  }

  .lien-hero__proof-text {
    font-family: var(--lien-font-body);
    font-size: var(--lien-text-sm);
    color: var(--lien-text-muted);
  }
{%- endstyle -%}

<section class="lien-hero" id="lien-hero-{{ section.id }}">
  <div class="lien-hero__inner">
    {%- comment -%} Image Side {%- endcomment -%}
    <div class="lien-hero__media">
      {%- if section.settings.image != blank -%}
        {{
          section.settings.image
          | image_url: width: 1200
          | image_tag:
            class: 'lien-hero__image',
            loading: 'eager',
            sizes: '(min-width: 1024px) 50vw, 100vw',
            widths: '375, 550, 750, 1000, 1200'
        }}
      {%- else -%}
        <div class="lien-hero__placeholder">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
          </svg>
        </div>
      {%- endif -%}
    </div>

    {%- comment -%} Content Side {%- endcomment -%}
    <div class="lien-hero__content">
      {%- if section.settings.label != blank -%}
        <span class="lien-hero__label">{{ section.settings.label }}</span>
      {%- endif -%}

      <h1 class="lien-hero__title">{{ section.settings.heading }}</h1>

      {%- if section.settings.subtitle != blank -%}
        <p class="lien-hero__subtitle">{{ section.settings.subtitle }}</p>
      {%- endif -%}

      <div class="lien-hero__actions">
        {%- if section.settings.button_label_1 != blank -%}
          <a href="{{ section.settings.button_link_1 }}" class="lien-btn lien-btn--accent">
            {{ section.settings.button_label_1 }}
          </a>
        {%- endif -%}

        {%- if section.settings.button_label_2 != blank -%}
          <a href="{{ section.settings.button_link_2 }}" class="lien-btn lien-btn--outline">
            {{ section.settings.button_label_2 }}
          </a>
        {%- endif -%}
      </div>

      {%- if section.settings.show_social_proof -%}
        <div class="lien-hero__proof">
          <div class="lien-hero__proof-stars">
            {%- for i in (1..5) -%}
              <svg viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            {%- endfor -%}
          </div>
          <span class="lien-hero__proof-text">{{ section.settings.social_proof_text }}</span>
        </div>
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Lien Hero",
  "tag": "section",
  "class": "lien-hero-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Hero image"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Label",
      "default": "Made for Two"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading",
      "default": "Wear Your Bond"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Magnetic couple bracelets that keep you connected — no matter the distance. One set, two souls."
    },
    {
      "type": "text",
      "id": "button_label_1",
      "label": "Primary button label",
      "default": "Shop the Set"
    },
    {
      "type": "url",
      "id": "button_link_1",
      "label": "Primary button link"
    },
    {
      "type": "text",
      "id": "button_label_2",
      "label": "Secondary button label",
      "default": "Our Story"
    },
    {
      "type": "url",
      "id": "button_link_2",
      "label": "Secondary button link"
    },
    {
      "type": "checkbox",
      "id": "show_social_proof",
      "label": "Show social proof",
      "default": true
    },
    {
      "type": "text",
      "id": "social_proof_text",
      "label": "Social proof text",
      "default": "Loved by 50,000+ couples worldwide"
    }
  ],
  "presets": [
    {
      "name": "Lien Hero"
    }
  ]
}
{% endschema %}
