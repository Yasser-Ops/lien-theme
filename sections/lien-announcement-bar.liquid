{%- style -%}
  /* ======================================================================
     Lien Announcement Bar
     Dark top bar with rotating promotional messages
     ====================================================================== */

  .lien-announcement {
    background-color: var(--lien-dark);
    overflow: hidden;
    position: relative;
  }

  .lien-announcement__inner {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem var(--lien-container-padding-mobile);
    position: relative;
    min-height: 3.8rem;
  }

  @media screen and (min-width: 768px) {
    .lien-announcement__inner {
      padding: 1rem var(--lien-container-padding);
    }
  }

  /* Slide track */
  .lien-announcement__track {
    overflow: hidden;
    flex: 1;
    text-align: center;
  }

  /* Single message */
  .lien-announcement__message {
    display: none;
    font-family: var(--lien-font-body);
    font-size: var(--lien-text-xs);
    font-weight: var(--lien-weight-medium);
    letter-spacing: 0.06em;
    color: var(--lien-offwhite);
    line-height: 1.4;
    animation: lien-ann-in 400ms ease forwards;
  }

  .lien-announcement__message.is-active {
    display: block;
  }

  @keyframes lien-ann-in {
    from { opacity: 0; transform: translateY(6px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .lien-announcement__message a {
    color: var(--lien-accent-light);
    text-decoration: underline;
    text-underline-offset: 3px;
    transition: color var(--lien-transition-fast);
  }

  .lien-announcement__message a:hover {
    color: var(--lien-offwhite);
  }

  /* Gold accent dot between messages */
  .lien-announcement__message .lien-ann-accent {
    color: var(--lien-accent);
    font-weight: var(--lien-weight-bold);
  }

  /* Dismiss button */
  .lien-announcement__close {
    position: absolute;
    right: 1.2rem;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    cursor: pointer;
    color: rgba(250, 250, 248, 0.4);
    padding: 0.4rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: color var(--lien-transition-fast);
  }

  .lien-announcement__close:hover {
    color: var(--lien-offwhite);
  }

  .lien-announcement__close svg {
    width: 1.4rem;
    height: 1.4rem;
    stroke: currentColor;
    fill: none;
    stroke-width: 2;
    stroke-linecap: round;
  }

  /* Dot pagination */
  .lien-announcement__dots {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    position: absolute;
    left: 50%;
    bottom: 0.5rem;
    transform: translateX(-50%);
  }

  .lien-announcement__dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background-color: rgba(250, 250, 248, 0.25);
    transition: background-color var(--lien-transition-fast);
    cursor: pointer;
    border: none;
    padding: 0;
  }

  .lien-announcement__dot.is-active {
    background-color: var(--lien-accent);
  }

  /* Hide dots when only one message */
  .lien-announcement--single .lien-announcement__dots {
    display: none;
  }

  /* Extra bottom padding when dots are shown */
  .lien-announcement:not(.lien-announcement--single) .lien-announcement__inner {
    padding-bottom: 1.8rem;
  }
{%- endstyle -%}

{%- if section.blocks.size > 0 -%}
  <div
    class="lien-announcement{% if section.blocks.size == 1 %} lien-announcement--single{% endif %}"
    id="lien-announcement-{{ section.id }}"
    role="region"
    aria-label="Announcements"
  >
    <div class="lien-announcement__inner">

      <div class="lien-announcement__track">
        {%- for block in section.blocks -%}
          <p
            class="lien-announcement__message{% if forloop.first %} is-active{% endif %}"
            data-ann-msg
            {{ block.shopify_attributes }}
          >
            {%- if block.settings.link != blank -%}
              <a href="{{ block.settings.link }}">{{ block.settings.text }}</a>
            {%- else -%}
              {{ block.settings.text }}
            {%- endif -%}
          </p>
        {%- endfor -%}
      </div>

      {%- if section.settings.show_close -%}
        <button class="lien-announcement__close" aria-label="Dismiss announcement" data-ann-close>
          <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
        </button>
      {%- endif -%}

      {%- if section.blocks.size > 1 -%}
        <div class="lien-announcement__dots" aria-hidden="true">
          {%- for block in section.blocks -%}
            <button
              class="lien-announcement__dot{% if forloop.first %} is-active{% endif %}"
              data-ann-dot="{{ forloop.index0 }}"
            ></button>
          {%- endfor -%}
        </div>
      {%- endif -%}

    </div>
  </div>

  <script>
  (function () {
    var bar = document.getElementById('lien-announcement-{{ section.id }}');
    if (!bar) return;

    var messages = bar.querySelectorAll('[data-ann-msg]');
    var dots = bar.querySelectorAll('[data-ann-dot]');
    var closeBtn = bar.querySelector('[data-ann-close]');
    var current = 0;
    var timer = null;
    var interval = {{ section.settings.rotation_speed | times: 1000 | default: 4000 }};

    function show(index) {
      messages.forEach(function (m, i) {
        m.classList.toggle('is-active', i === index);
      });
      dots.forEach(function (d, i) {
        d.classList.toggle('is-active', i === index);
      });
      current = index;
    }

    function next() {
      show((current + 1) % messages.length);
    }

    function startTimer() {
      if (messages.length < 2) return;
      timer = setInterval(next, interval);
    }

    function stopTimer() {
      clearInterval(timer);
    }

    dots.forEach(function (dot) {
      dot.addEventListener('click', function () {
        stopTimer();
        show(parseInt(dot.dataset.annDot, 10));
        startTimer();
      });
    });

    if (closeBtn) {
      closeBtn.addEventListener('click', function () {
        stopTimer();
        bar.style.display = 'none';
      });
    }

    startTimer();
  })();
  </script>
{%- endif -%}

{% schema %}
{
  "name": "Lien Announcement Bar",
  "tag": "div",
  "class": "lien-announcement-wrapper",
  "settings": [
    {
      "type": "range",
      "id": "rotation_speed",
      "label": "Rotation speed (seconds)",
      "min": 2,
      "max": 8,
      "step": 1,
      "default": 4
    },
    {
      "type": "checkbox",
      "id": "show_close",
      "label": "Show dismiss button",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "message",
      "name": "Message",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Message",
          "default": "Free shipping on orders over $50 — use code FREESHIP"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link (optional)",
          "info": "Makes the entire message a clickable link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Lien Announcement Bar",
      "blocks": [
        {
          "type": "message",
          "settings": {
            "text": "✨ Free shipping on all orders over $50"
          }
        },
        {
          "type": "message",
          "settings": {
            "text": "New customer? Get 15% off your first order — use code LIEN15"
          }
        },
        {
          "type": "message",
          "settings": {
            "text": "30-day money-back guarantee · No questions asked"
          }
        }
      ]
    }
  ]
}
{% endschema %}
