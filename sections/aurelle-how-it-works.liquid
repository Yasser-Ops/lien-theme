{%- style -%}
  /* ======================================================================
     Aurelle How It Works
     Numbered step cards
     ====================================================================== */

  .aurelle-how-it-works {
    background-color: var(--aurelle-white);
  }

  .aurelle-how-it-works__inner {
    max-width: var(--aurelle-container-max);
    margin: 0 auto;
    padding: var(--aurelle-section-padding-mobile) var(--aurelle-container-padding-mobile);
  }

  @media screen and (min-width: 768px) {
    .aurelle-how-it-works__inner {
      padding: var(--aurelle-section-padding-desktop) var(--aurelle-container-padding);
    }
  }

  .aurelle-how-it-works__header {
    text-align: center;
    margin-bottom: var(--aurelle-space-3xl);
  }

  .aurelle-how-it-works__label {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-semibold);
    font-size: var(--aurelle-text-xs);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--aurelle-gold);
    display: block;
    margin-bottom: var(--aurelle-space-sm);
  }

  .aurelle-how-it-works__title {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-bold);
    font-size: var(--aurelle-text-3xl);
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--aurelle-text);
    margin: 0 0 var(--aurelle-space-md) 0;
  }

  .aurelle-how-it-works__subtitle {
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-base);
    color: var(--aurelle-text-secondary);
    line-height: 1.7;
    max-width: 560px;
    margin: 0 auto;
  }

  .aurelle-how-it-works__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--aurelle-space-xl);
  }

  @media screen and (min-width: 768px) {
    .aurelle-how-it-works__grid {
      grid-template-columns: repeat({{ section.blocks.size | at_most: 4 }}, 1fr);
      gap: var(--aurelle-space-2xl);
    }
  }

  .aurelle-how-it-works__step {
    text-align: center;
    padding: var(--aurelle-space-xl) var(--aurelle-space-lg);
    position: relative;
  }

  .aurelle-how-it-works__number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background-color: var(--aurelle-cream);
    color: var(--aurelle-gold);
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-bold);
    font-size: var(--aurelle-text-lg);
    margin-bottom: var(--aurelle-space-lg);
    transition: all var(--aurelle-transition-base);
  }

  .aurelle-how-it-works__step:hover .aurelle-how-it-works__number {
    background-color: var(--aurelle-gold);
    color: var(--aurelle-white);
    transform: scale(1.05);
  }

  .aurelle-how-it-works__icon {
    width: 40px;
    height: 40px;
    margin: 0 auto var(--aurelle-space-md);
    color: var(--aurelle-gold);
    opacity: 0.7;
  }

  .aurelle-how-it-works__icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .aurelle-how-it-works__step-title {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-semibold);
    font-size: var(--aurelle-text-lg);
    color: var(--aurelle-text);
    margin: 0 0 var(--aurelle-space-sm) 0;
  }

  .aurelle-how-it-works__step-text {
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-sm);
    color: var(--aurelle-text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* Connector line between steps (desktop only) */
  @media screen and (min-width: 768px) {
    .aurelle-how-it-works__step:not(:last-child)::after {
      content: '';
      position: absolute;
      top: 46px;
      right: -16px;
      width: 32px;
      height: 1px;
      background-color: var(--aurelle-border);
    }
  }
{%- endstyle -%}

<section class="aurelle-how-it-works" id="aurelle-how-it-works-{{ section.id }}">
  <div class="aurelle-how-it-works__inner">
    <div class="aurelle-how-it-works__header">
      {%- if section.settings.label != blank -%}
        <span class="aurelle-how-it-works__label">{{ section.settings.label }}</span>
      {%- endif -%}
      <h2 class="aurelle-how-it-works__title">{{ section.settings.heading }}</h2>
      {%- if section.settings.subtitle != blank -%}
        <p class="aurelle-how-it-works__subtitle">{{ section.settings.subtitle }}</p>
      {%- endif -%}
    </div>

    <div class="aurelle-how-it-works__grid">
      {%- for block in section.blocks -%}
        <div class="aurelle-how-it-works__step" {{ block.shopify_attributes }}>
          <div class="aurelle-how-it-works__number">{{ forloop.index }}</div>

          {%- if block.settings.icon != blank -%}
            <div class="aurelle-how-it-works__icon">
              {{ block.settings.icon | image_url: width: 80 | image_tag: loading: 'lazy' }}
            </div>
          {%- endif -%}

          <h3 class="aurelle-how-it-works__step-title">{{ block.settings.title }}</h3>
          <p class="aurelle-how-it-works__step-text">{{ block.settings.description }}</p>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Aurelle How It Works",
  "tag": "section",
  "class": "aurelle-how-it-works-section",
  "settings": [
    {
      "type": "text",
      "id": "label",
      "label": "Label",
      "default": "Simple Routine"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "How It Works"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Three simple steps to radiant, healthy skin every day."
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon image (optional)"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Step title",
          "default": "Step Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Step description",
          "default": "Describe this step in your skincare routine."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Aurelle How It Works",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "title": "Cleanse",
            "description": "Start with a gentle cleanse to prepare your skin for maximum absorption."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Apply",
            "description": "Apply a small amount to your face and neck using gentle upward motions."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Glow",
            "description": "See visible results within weeks as your skin transforms naturally."
          }
        }
      ]
    }
  ]
}
{% endschema %}
