{%- style -%}
  /* ======================================================================
     Aurelle Reviews
     Social proof review cards
     ====================================================================== */

  .aurelle-reviews {
    background-color: var(--aurelle-cream);
  }

  .aurelle-reviews__inner {
    max-width: var(--aurelle-container-max);
    margin: 0 auto;
    padding: var(--aurelle-section-padding-mobile) var(--aurelle-container-padding-mobile);
  }

  @media screen and (min-width: 768px) {
    .aurelle-reviews__inner {
      padding: var(--aurelle-section-padding-desktop) var(--aurelle-container-padding);
    }
  }

  .aurelle-reviews__header {
    text-align: center;
    margin-bottom: var(--aurelle-space-3xl);
  }

  .aurelle-reviews__label {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-semibold);
    font-size: var(--aurelle-text-xs);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--aurelle-gold);
    display: block;
    margin-bottom: var(--aurelle-space-sm);
  }

  .aurelle-reviews__title {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-bold);
    font-size: var(--aurelle-text-3xl);
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--aurelle-text);
    margin: 0 0 var(--aurelle-space-md) 0;
  }

  .aurelle-reviews__subtitle {
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-base);
    color: var(--aurelle-text-secondary);
    line-height: 1.7;
    max-width: 560px;
    margin: 0 auto;
  }

  /* Card Grid */
  .aurelle-reviews__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--aurelle-space-lg);
  }

  @media screen and (min-width: 768px) {
    .aurelle-reviews__grid {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--aurelle-space-xl);
    }
  }

  /* Mobile horizontal scroll */
  @media screen and (max-width: 767px) {
    .aurelle-reviews__grid {
      display: flex;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
      gap: var(--aurelle-space-md);
      padding-bottom: var(--aurelle-space-md);
      scrollbar-width: none;
    }

    .aurelle-reviews__grid::-webkit-scrollbar {
      display: none;
    }

    .aurelle-reviews__card {
      min-width: 85vw;
      scroll-snap-align: start;
      flex-shrink: 0;
    }
  }

  .aurelle-reviews__card {
    background-color: var(--aurelle-white);
    padding: var(--aurelle-space-xl);
    display: flex;
    flex-direction: column;
    gap: var(--aurelle-space-md);
    transition: all var(--aurelle-transition-base);
  }

  .aurelle-reviews__card:hover {
    transform: translateY(-3px);
    box-shadow: var(--aurelle-shadow-lg);
  }

  /* Stars */
  .aurelle-reviews__stars {
    display: flex;
    gap: 3px;
  }

  .aurelle-reviews__star {
    width: 16px;
    height: 16px;
    color: var(--aurelle-gold);
  }

  .aurelle-reviews__star--empty {
    color: var(--aurelle-border);
  }

  .aurelle-reviews__star svg {
    width: 100%;
    height: 100%;
    fill: currentColor;
  }

  /* Review text */
  .aurelle-reviews__text {
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-base);
    color: var(--aurelle-text-secondary);
    line-height: 1.7;
    margin: 0;
    flex-grow: 1;
  }

  /* Reviewer info */
  .aurelle-reviews__reviewer {
    display: flex;
    align-items: center;
    gap: var(--aurelle-space-sm);
    padding-top: var(--aurelle-space-md);
    border-top: 1px solid var(--aurelle-border-light);
  }

  .aurelle-reviews__avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: var(--aurelle-cream);
    overflow: hidden;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .aurelle-reviews__avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .aurelle-reviews__avatar-initial {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-semibold);
    font-size: var(--aurelle-text-sm);
    color: var(--aurelle-gold);
  }

  .aurelle-reviews__name {
    font-family: var(--aurelle-font-heading);
    font-weight: var(--aurelle-weight-semibold);
    font-size: var(--aurelle-text-sm);
    color: var(--aurelle-text);
  }

  .aurelle-reviews__verified {
    display: flex;
    align-items: center;
    gap: 4px;
    font-family: var(--aurelle-font-body);
    font-size: var(--aurelle-text-xs);
    color: var(--aurelle-gold);
  }

  .aurelle-reviews__verified svg {
    width: 14px;
    height: 14px;
    fill: currentColor;
  }
{%- endstyle -%}

<section class="aurelle-reviews" id="aurelle-reviews-{{ section.id }}">
  <div class="aurelle-reviews__inner">
    <div class="aurelle-reviews__header">
      {%- if section.settings.label != blank -%}
        <span class="aurelle-reviews__label">{{ section.settings.label }}</span>
      {%- endif -%}
      <h2 class="aurelle-reviews__title">{{ section.settings.heading }}</h2>
      {%- if section.settings.subtitle != blank -%}
        <p class="aurelle-reviews__subtitle">{{ section.settings.subtitle }}</p>
      {%- endif -%}
    </div>

    <div class="aurelle-reviews__grid">
      {%- for block in section.blocks -%}
        <div class="aurelle-reviews__card" {{ block.shopify_attributes }}>
          {%- comment -%} Star Rating {%- endcomment -%}
          <div class="aurelle-reviews__stars">
            {%- assign rating = block.settings.rating | default: 5 -%}
            {%- for i in (1..5) -%}
              <span class="aurelle-reviews__star {% if i > rating %}aurelle-reviews__star--empty{% endif %}">
                <svg viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              </span>
            {%- endfor -%}
          </div>

          {%- comment -%} Review Text {%- endcomment -%}
          <p class="aurelle-reviews__text">"{{ block.settings.review_text }}"</p>

          {%- comment -%} Reviewer {%- endcomment -%}
          <div class="aurelle-reviews__reviewer">
            <div class="aurelle-reviews__avatar">
              {%- if block.settings.avatar != blank -%}
                {{ block.settings.avatar | image_url: width: 80 | image_tag: loading: 'lazy' }}
              {%- else -%}
                <span class="aurelle-reviews__avatar-initial">{{ block.settings.name | slice: 0 | upcase }}</span>
              {%- endif -%}
            </div>
            <div>
              <div class="aurelle-reviews__name">{{ block.settings.name }}</div>
              {%- if block.settings.verified -%}
                <span class="aurelle-reviews__verified">
                  <svg viewBox="0 0 24 24"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><path d="M22 4L12 14.01l-3-3" fill="none" stroke="currentColor" stroke-width="2"/></svg>
                  Verified Purchase
                </span>
              {%- endif -%}
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Aurelle Reviews",
  "tag": "section",
  "class": "aurelle-reviews-section",
  "settings": [
    {
      "type": "text",
      "id": "label",
      "label": "Label",
      "default": "Real Results"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "What Our Customers Say"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Join thousands who have transformed their skincare routine."
    }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "Review",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "Reviewer name",
          "default": "Sarah M."
        },
        {
          "type": "range",
          "id": "rating",
          "label": "Rating",
          "min": 1,
          "max": 5,
          "step": 1,
          "default": 5
        },
        {
          "type": "textarea",
          "id": "review_text",
          "label": "Review text",
          "default": "I've never felt so confident in my skin. The results are truly visible."
        },
        {
          "type": "checkbox",
          "id": "verified",
          "label": "Verified purchase",
          "default": true
        },
        {
          "type": "image_picker",
          "id": "avatar",
          "label": "Avatar image (optional)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Aurelle Reviews",
      "blocks": [
        {
          "type": "review",
          "settings": {
            "name": "Sarah M.",
            "rating": 5,
            "review_text": "I've been using this for three weeks and the difference is remarkable. My skin feels softer, looks brighter, and the fine lines around my eyes have visibly diminished.",
            "verified": true
          }
        },
        {
          "type": "review",
          "settings": {
            "name": "Elena K.",
            "rating": 5,
            "review_text": "Finally a product that delivers on its promises. The texture is divine and a little goes a long way. My morning routine has never felt so luxurious.",
            "verified": true
          }
        },
        {
          "type": "review",
          "settings": {
            "name": "Priya D.",
            "rating": 5,
            "review_text": "As someone with sensitive skin, I'm always cautious. This formula is gentle yet effective. No irritation, just beautiful, healthy-looking skin.",
            "verified": true
          }
        }
      ]
    }
  ]
}
{% endschema %}
